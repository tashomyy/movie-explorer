export enum Theme {
  Light = "light",
  Dark = "dark",
}
export enum SearchType {
  Name = "name",
  Genre = "genre",
  Year = "year",
}
export enum MovieSectionType {
  Popular = "popular",
  Trending = "trending",
  Streaming = "streaming",
  Upcoming = "upcoming",
}
